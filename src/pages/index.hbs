<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <title>患者スケジュール表</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
  <style>
    table { border-collapse: collapse; width: 100%; }
    th, td { border: 1px solid #ccc; padding: 6px; text-align: center; }
    .editable:hover { background-color: #eef; cursor: pointer; }
    #barChartContainer { margin-top: 40px; }
  </style>
</head>
<body class="p-4">
  <h1>🗓️ インタラクティブ時間割（09:00〜20:00）</h1>

  <div class="mb-3">
    <button class="btn btn-primary" onclick="addNewCK()">＋ 新しい患者を追加</button>
  </div>

  <select id="clinicSelector" class="form-select mb-3" onchange="switchClinic(this.value)">
    {{#each clinics}}
      <option value="{{this.id}}">{{this.name}}</option>
    {{/each}}
  </select>

  <div id="timetable-container">
    {{{timetableHtml}}}
  </div>

  <div id="barChartContainer">
    <h2>📊 スケジュール時間帯チャート</h2>
    <canvas id="barChart" height="300"></canvas>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script src="/script.js"></script>
</body>
</html>
